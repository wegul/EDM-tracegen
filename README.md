YAPS = Yet Another Packet Simulator
==================================

# 1. Build
Inside simulator/ directory

    $ autoconf
    $ autoreconf -i
    $ ./configure
    $ make


# 2. Generate traces
    $ cd ./EDM-workload
    $ ./generate_traces.sh

